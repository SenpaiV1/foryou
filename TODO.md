# TODO: Fix Web Project Errors

- [x] Update image paths in gallery.html to use './albums/' for relative paths
- [x] Update image paths in gallery.js to use './her/' for relative paths
- [x] Change autoplay behavior in musicplayer.js: modify ended event to selectTrack(0) instead of selectTrack(1) to avoid NotAllowedError
- [x] Add check in musicplayer.js to ensure audio.src is set before calling play()
